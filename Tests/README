Test files for different disassemblers
∙	Debug.bin: the DOS DEBUG.EXE, after self-extraction (it unpacks, extracts and loads itself before executing).
∙	DebugA.s: disassembly from ndisasm.
∙	DebugB.s: disassembly from Dis86, via DOS Box, after conversion from DOS to POSIX newlines.
